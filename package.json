{
  "name": "lin-lab-new-student-handbook",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "node --test tests/*.test.mjs",
    "verify": "node scripts/verify-bootcamp.mjs && npm run test && npm run build"
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.0",
    "@astrojs/starlight": "^0.41.0",
    "astro": "^7.0.2"
  }
}
